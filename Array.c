#include <stdio.h>
#include <string.h>

void Array()
{


    /*
    char hello[5] = {'h', 'e', 'l', 'l', 'o'};

    size_t size = sizeof(hello);
    
    for (int i = 0, j = size - 1; i < j; i++, j--) {
        char c = str[i];
        char original[] = {i};
        char reversed[] = {j};

        char c = original[i];
        original[i] = reversed[j];
        reversed[j] = c;
    }

    printf("original - %s \n", original);
    printf("reversed - %s \n", reversed);
    */

}

